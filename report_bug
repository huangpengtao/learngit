1 BaseScheduler
        if not (filtered_agents_dict and num_agents):
            return []
2 generate_dvr_port_status 

3 _get_agent_fdb  delete no used variable agent_host


3 update_port_postcommit ()

4  Plugin.py (plugins\ml2) delete_port use context.session

5 remove_networks_from_down_agents
   LOG.warn(_LW("No DHCP agents available, "
   LOG.debug("No DHCP agents available, "   
change log level warn to debug
to avoid so frequent same log which is not very usefull
   
